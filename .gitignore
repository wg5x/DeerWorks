# Vendor projects are independent upstream repositories.
# Keep them out of this top-level project's git history so they can be upgraded directly.
/vendor/deer-flow/
/vendor/agentscope/
/vendor/agentscope-runtime/

# Local runtime/config files
.DS_Store
.env
.env.*
!.env.example
